Feature: Auth

Structure:
- pages/ : route-level screens for auth flows (login, register, forgot password).
- components/ : reusable UI pieces used by auth pages.
- services/ : API calls for auth (login, refresh, profile).
- state/ : local/store state (e.g., Redux slices or Zustand stores).
- utils/ : helpers (token storage, validators).
